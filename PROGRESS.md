# 작업 진행 상황 (단어 시험지 만들기)

마지막 업데이트: 2026-07-26 (빈 열 버그 수정 + 가로 A4 2단 인쇄 옵션)

> 코드 구조와 지켜야 할 제약은 [CLAUDE.md](CLAUDE.md), 사용법은 [README.md](README.md),
> 엔진 비교 결과는 [bench/BENCHMARK.md](bench/BENCHMARK.md)에 있습니다.
> 이 파일은 "지금까지 무엇을 했고 무엇이 남았는지"만 적습니다.

## 프로젝트 개요

사진 속 영어/한글 혼합 단어표를 인식해서
- 영어만 보고 한글 뜻을 맞히는 시험지
- 한글만 보고 영어를 맞히는 시험지

를 만들어 인쇄하거나 PDF로 저장하는 Windows용 Electron 앱. 경로: `C:\Users\jsj\study` (git 저장소, `main` 브랜치)

## 실행 방법

```
npm install   # 최초 1회
npm start     # tsc 컴파일 후 Electron 실행
```

바탕화면 **"단어 시험지 만들기"** 아이콘으로도 실행 가능(`launch.vbs`).
단, `launch.vbs`는 TypeScript를 컴파일하지 않으므로 `.ts`를 수정했다면 `npm run build`를 먼저 해야 합니다.

## 완료된 작업

### 1. 기본 앱 (초기)
업로드(드래그앤드롭/여러 장) → 진행률 표시 → 표 인식 → 편집(셀 수정·열 언어 변경·행 삭제) →
미리보기 2탭(영어 전용 / 한글 전용, 세로 A4 · 표 1개당 1페이지) → 인쇄 / PDF 저장.
제목 기본값은 파일명이 아니라 "영어 단어 테스트".

### 2. TypeScript 전환
`main.ts`, `preload.ts`, `src/vlm.ts`, `renderer/renderer.ts` + 공용 타입 `src/types.d.ts`.
컴파일 결과 `.js`를 소스 옆에 생성하며 둘 다 커밋합니다.

### 3. PaddleOCR 지원 추가 (기본값)
- `scripts/paddle_table.py` — PP-StructureV3로 표를 인식해 셀 그리드 JSON을 출력. PaddleOCR 3.x/2.x 모두 지원.
- `src/paddle.ts` — 이미지를 임시 폴더에 쓰고 파이썬을 실행, 결과를 `TableData`로 변환.
  OCR은 글자만 주므로 열의 영어/한글 구분과 헤더 행 판정을 여기서 합니다.
- 설정 UI에 "PaddleOCR (로컬 OCR, 기본값)" + Python 경로 입력란 추가.
- 실제 샘플 사진 정확도: 영어 38~39/40, 한글 39~40/40 (약 95~98%).

### 4. 엔진 비교 벤치마크 (`bench/`)
정답을 정확히 알아야 채점이 가능해서, 실제 사진 대신 표 이미지 100장을 직접 생성했습니다
(레이아웃 6종 · 폰트 4종 · 글자 11~65px · 테두리 3종 무작위 조합, 단어쌍 1913개).

| 엔진 | 단어쌍 정확도 | 장당 |
| --- | --- | --- |
| OpenAI gpt-4o | 99.5% | 3.1초 |
| Ollama qwen2.5vl | 91.4% | 4.7초 |
| PaddleOCR (기본값) | 80.1% | 29.2초 |
| Ollama qwen3-vl:8b | 77.8% | 45.2초 |
| Tesseract | 54.2% | 0.8초 |

### 5. 연구 논문 전사 벤치마크 (`bench-papers/`)
표 벤치마크의 약점(단어 목록 174쌍이 고정돼 어휘가 편향됨)을 보완하려고, 실제 연구 논문
PDF 100건(arXiv 영어 50 + KoreaMed Synapse 한글 혼합 50)에서 각 1페이지를 200 DPI로
렌더링해 **페이지 전체 텍스트 전사 정확도**를 측정했습니다. 정답은 PDF 텍스트 레이어입니다.

| 엔진 | CER | 영문자 CER | 한글 CER | 단어 F1 | 장당 |
| --- | --- | --- | --- | --- | --- |
| Ollama qwen2.5vl | **10.9%** | 7.7% | 5.1% | 93.8% | 17.3초 |
| OpenAI gpt-4o | 20.8% | 16.6% | 24.0% | 82.8% | 8.5초 |
| Tesseract | 27.4% | 22.3% | 48.0% | 71.0% | 3.5초 |
| PaddleOCR | 47.8% | 29.0% | 59.0% | **94.0%** | 51.7초 |

여기서 나온 큰 발견 두 가지:

- **OpenAI gpt-4o가 100페이지 중 51페이지를 전사 거부**했습니다("I can't assist with that").
  출판물 전문 복제로 판단하는 듯하며 영어·한글 양쪽에서 발생합니다. 거부는 인식 실패와
  성질이 달라 정확도 평균에서 제외하고 따로 셌습니다.
- **PaddleOCR은 단어 F1이 1위(94.0%)인데 CER은 최하위(47.8%)** 입니다. 글자는 가장 잘
  읽지만 2단 조판에서 좌우 단을 한 줄씩 번갈아 이어 붙여 문장 순서가 깨집니다.
  → 레이아웃을 해석하는 PP-StructureV3로 바꾸면 대부분 해소될 것으로 보입니다(미검증).

`qwen3-vl:8b`는 시험 실행에서 한글 페이지 1장에 110초, 정답의 4.7배 분량을 출력하는
반복 루프에 빠져 이 벤치마크에서는 제외했습니다(표 벤치마크에는 포함).

### 6. 인쇄 형식 선택 추가 (가로 A4 2단)
미리보기 상단에 **인쇄 형식** 드롭다운을 추가했습니다. 기존 `세로 A4 · 한 장에 표 1개`에 더해
`가로 A4 · 한 장에 표 2개`를 고르면 가로 A4에 표를 좌우로 2개 배치합니다. 블록은 올린 사진 순서대로
채우므로 사진 2장이 한 페이지에 들어가고, 1장뿐이거나 행이 많으면 그 표를 균등하게 나눠 좌우를
채웁니다(한 칸 최대 21행). 표가 페이지 높이(170mm)에 맞춰 늘어나 답 쓰는 칸도 커집니다.
용지 방향은 선택한 형식에 따라 자동으로 `printNow`/`exportPdf`에 전달됩니다.

실제 렌더러를 Electron으로 구동해 확인한 결과: 사진 1장 → 1페이지(10행+10행), 2장 → 1페이지
(20행+20행, 파일명 캡션 표시), 3장 → 2페이지, 세로 형식은 기존과 동일. 모두 PDF 페이지 수까지 확인.

## 해결한 버그

초기 3건(바탕화면 아이콘 안 뜸 / 설정 저장 피드백 없음 / 설정 모달 안 닫힘 — `.modal-backdrop[hidden]`)
외에 이후 발견한 것들:

1. **PDF가 빈 페이지로 저장됨** — 저장 다이얼로그가 창을 가리면 Chromium이 렌더링을 중단해서
   `printToPDF`가 빈 프레임을 캡처. `backgroundThrottling: false` + 저장 후 `focus()` & 150ms 대기로 해결.
2. **인쇄물 가장자리가 잘림** — `.sheet`가 이미 210mm 전체 폭인데 `@page`/`printToPDF`가 여백을 더해
   인쇄 영역을 줄였음. 양쪽 모두 여백 0으로 고정.
3. **VLM이 순번 열을 빼먹어 열이 한 칸씩 밀림** — `repairMissingLeadingNumberColumn()`으로 좁은
   조건에서만 복구(모든 행이 정확히 1개 더 많고, `other` 열이 없고, 첫 칸이 모두 숫자일 때).
4. **PaddleOCR이 셀을 180° 회전해서 읽음** (`amazing`→`buzeue`, `12`→`Z1`) — 텍스트라인 방향
   분류기가 짧은 셀을 뒤집힌 줄로 오판. `use_textline_orientation=False`로 벤치마크 56%→80%,
   실제 샘플 93%→95~98%로 개선. **이번 작업에서 가장 큰 발견.**
5. **PaddleOCR이 추론 중 크래시** — oneDNN 경로 버그. `enable_mkldnn=False`로 우회.
6. **`pip install paddleocr`만으로는 표 인식 불가** — PP-StructureV3에 `doc-parser` extra 필요.
7. **한글 시험지의 3번째 열이 통째로 비어서 인쇄됨** (2026-07-26) — 인식/인쇄 로직 문제가 아니라
   `qwen2.5vl`이 좌우 2블록 표의 **맨 오른쪽 열**을 `columns`에는 선언해 놓고 모든 행에서 `""`로
   돌려준 것이었습니다(샘플 1·2 모두 재현). 두 가지로 고쳤습니다.
   - VLM에 보내기 전 이미지를 업스케일(`src/image.ts`, 긴 변 2200px·최대 3배). 샘플 1은 이것만으로
     해결(653×736 → 1952×2200). 프롬프트로 "빈 열을 만들지 말라"고 지시하는 건 효과 없었습니다.
   - 그래도 비는 열이 있으면(샘플 2가 그랬음) 그 열 하나만 다시 물어보는 2차 요청
     (`repairBlankColumns()`). 행 수가 정확히 맞을 때만 병합합니다. 재읽기 결과는 PaddleOCR 결과와
     20개 값이 완전히 일치했습니다.

     원인 추적 과정과 배제한 가설, 시도별 결과는 **[docs/blank-column-bug.md](docs/blank-column-bug.md)** 에 따로 정리했습니다.

## 남은 것 / 제안

- **기본 엔진 재검토**: 이 PC(GPU 있음)에서는 Ollama `qwen2.5vl`이 PaddleOCR보다 11pp 정확하고
  6배 빠르면서 똑같이 오프라인·무료입니다. 사용자가 PaddleOCR을 기본값으로 지정했으므로 그대로
  두었지만, 바꿀지 결정이 필요합니다. (GPU 없는 PC라면 PaddleOCR이 여전히 합리적)
- **테두리 없는 표 대응**: PaddleOCR 실패 10장이 전부 테두리 없는 표였습니다. 표 검출이 실패하면
  일반 OCR로 글자를 읽고 좌표로 행·열을 재구성하는 폴백을 넣으면 해결됩니다
  (`bench/runners.py`의 Tesseract 재구성 로직이 그대로 참고가 됩니다).
- `npm run dist`로 NSIS 설치파일 만들기 — 아직 실행해보지 않았음
- 앱 전용 아이콘(.ico) 지정 (현재 Electron 기본 아이콘)
- 편집 화면에 행/열 추가 기능 (현재는 행 삭제만)
- 사진 한 장에 표가 여러 개인 경우 (현재는 1장 = 1표 가정)
